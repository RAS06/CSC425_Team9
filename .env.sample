# Copy this file to .env and fill in your values
PORT=3000
# For local MongoDB (uncomment to use local dev DB)
# MONGODB_URI=mongodb://localhost:27017/backend-example
# For MongoDB Atlas, use the connection string without credentials in the file
# Example (replace <username>, <password>, <cluster-url>, <dbname>):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority


#sample file genned by copilot